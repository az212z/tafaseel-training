# تقرير فحص منصة تفاصيل

تاريخ الفحص: 13 سبتمبر 2026. الواجهة React وTypeScript، والخدمات الخلفية مشروع Supabase مستقل. اختبارات المتصفح باستخدام Chromium وPlaywright وaxe-core؛ ليست اختبارات على أجهزة فعلية.

## البناء

- TypeScript وVite: بناء إنتاج ناجح.
- ESLint: ناجح دون أخطاء أو تحذيرات.
- Prettier: الملفات مطابقة للتنسيق.
- الترحيلات السبع مسجلة في قاعدة البيانات، ووظيفتا الحسابات والملفات منشورتان.

## اختبارات الخلفية

نجح 17 اختبار تكامل فعلي باستخدام حسابات مؤقتة، وليست استجابات محاكاة:

- منع الزائر من قراءة ملفات المتدربين والمدفوعات والدروس.
- عزل بيانات الحسابات ومنع منح الإدارة ذاتيًا أو تغيير حالة الحساب.
- إنشاء كورس ودرس ودفعة بصلاحية الإدارة.
- الطلب المعلق لا يفتح الدروس؛ القبول يفتح كورس المتدرب ودفعتَه فقط.
- فرض سعة الدفعة داخل معاملة قاعدة البيانات.
- حفظ تقدم التعلم ومنع الكتابة في حساب آخر.
- صلاحيات الرسوم والمدفوعات، ومنع التكرار وتجاوز المتبقي، بما يشمل طلبات متزامنة.
- الإلغاء المالي يحفظ الأثر، ولا يلغي مطالبة عليها تحصيل قائم.
- عزل رسائل المتدرب وردود الإدارة.
- حماية المرفقات وإيقاف وصول المتدرب الموقوف فورًا عبر وظيفة التحميل.
- رفض عمليات إدارة الحسابات للزائر والمتدرب.
- تغيير إلزامي لكلمة المرور المؤقتة وحماية حسابات الإدارة من إعادة التعيين بهذه الواجهة.
- إنشاء حساب متدرب حقيقي والتحقق من بياناته وكلمة مروره.

النتيجة المحلية التفصيلية في `output/backend/verification.json`، والاختبار القابل للتشغيل في `scripts/verify-backend.mjs`.

## الواجهة والتجاوب

- 15 مسارًا للإدارة والمتدرب، على عروض 1440 و390 و320 بكسل، في المظهرين الفاتح والداكن: **90 فحص تخطيط دون تجاوز أفقي**.
- 30 تدقيق وصول لهذه المسارات ضمن WCAG 2 A/AA و2.1 AA. عولجت قابلية التركيز على جدول سجل العمليات، ثم نجح تدقيقه مجددًا في المظهرين.
- 14 صفحة عامة وصفحة حساب، على المقاسات الثلاثة والمظهرين: **84 فحص تخطيط و28 تدقيق وصول**.
- فحص بصري للرئيسية، تسجيل الدخول، التسجيل، لوحة الإدارة، ملف المتدرب، لوحة المتدرب وصفحة الدرس على الكمبيوتر والجوال.
- فحص التدفقات: إنشاء حساب من الإدارة، تغيير كلمة المرور المؤقتة، إنشاء حساب ذاتيًا، طلب كورس، مراسلة الإدارة، تسجيل تحصيل، إضافة درس، إكمال درس وإلغاء إكماله وتحميل مرفقه.
- تحقق من جلسة مصادقة صالحة بعد تغيير كلمة المرور، ثم خروج ناجح.
- محاكاة تعذر حفظ خطة التعلم: إعادة المحاولة تحفظ التعديل في قاعدة البيانات، ويستمر بعد إعادة تحميل الصفحة. اختُبرت أيضًا تعديلات متتابعة للتحقق من اكتمال الحفظ قبل عرض حالته.

لا أخطاء JavaScript غير معالجة في جولات الفحص النهائية. فحص الوصول الآلي لا يضمن جميع حالات استخدام تقنيات المساعدة. لا تُنسب درجات Lighthouse الخاصة بالإصدار القديم إلى هذا الإصدار.

## النشر والتحقق المباشر

- نُشرت نسخة الواجهة `8cd88f5` بنجاح عبر [GitHub Actions](https://github.com/az212z/tafaseel-training/actions/runs/34756120318).
- الموقع الفعلي: https://az212z.github.io/tafaseel-training/
- طابقت بصمات الملفات العامة الـ47 نسخة `docs/` المحلية، بما يشمل HTML وJavaScript وCSS والخطوط والصور.
- تحقق مباشر لخمس صفحات عامة على الكمبيوتر والجوال، وتحويل الزائر من الإدارة إلى تسجيل الدخول.
- دخول فعلي إلى أقسام الإدارة التسعة وخروج ناجح، دون أخطاء تشغيل أو تجاوز أفقي.
- إنشاء متدرب من صفحة التسجيل العامة، وطلب كورس بحالة معلقة. محاولة فتح الإدارة أعادت المتدرب إلى حسابه، ثم نجح تسجيل الخروج.
- حُذفت جميع حسابات وملفات وبيانات الاختبار بعد التحقق. الحالة النهائية: حساب مالك واحد، أربعة كورسات، 12 درسًا، وصفر تسجيلات أو دفعات أو مطالبات أو تحصيل تجريبي.
- حساب المالك نشط، وتغيير كلمة المرور مطلوب في أول دخول. مفتاح الخدمة وكلمة مرور قاعدة البيانات وبيانات دخول المالك غير موجودة في الملفات المنشورة أو Git.

## حدود التشغيل

- الدخول والتسجيل وقاعدة البيانات حقيقية. SMTP مخصص غير مربوط؛ لا توجد رسائل تحقق أو استعادة تلقائية. الاستعادة بواسطة الإدارة بعد التحقق من الهوية.
- المدفوعات سجل تحصيل خارجي، وليست بوابة دفع إلكترونية أو فواتير ضريبية.
- الأربعة كورسات و12 درسًا محتوى تمهيدي قابل للتحرير. لا توجد مواعيد أو رسوم أو أعداد متدربين مفترضة.
- حُذفت حسابات وملفات الاختبار قبل التسليم النهائي. بيانات أول دخول للمالك محلية وخاصة وخارج Git.

تفاصيل التشغيل والصلاحيات في `BACKEND-HANDOFF.md`.
