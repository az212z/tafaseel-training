# منصة تفاصيل — دليل التشغيل والخدمات الخلفية

الواجهة: https://az212z.github.io/tafaseel-training/

الإدارة: https://az212z.github.io/tafaseel-training/#/admin

مشروع البيانات المستقل: `kjmhlixwoafuerhouxvz`، باسم `tafaseel-training`، على Supabase (الخطة المجانية، فرانكفورت). لم تُستخدم قاعدة بيانات مشروع آخر.

## ما نُفذ فعليًا

- Supabase Auth لتسجيل الحسابات بالبريد وكلمة المرور، وإدارة الجلسات وتغيير كلمة المرور.
- PostgreSQL مع Row Level Security لجميع جداول التطبيق العامة. صلاحية الإدارة في `admin_users`، ولا يمكن للمتدرب منحها لنفسه أو تغيير حالة حسابه.
- أسماء المتدربين وبيانات التواصل والمرحلة الدراسية، كورسات ودفعات ومقاعد وتسجيلات، دروس ونسبة إكمال، رسوم وأقساط وسجل تحصيل، رسائل وإعلانات وسجل عمليات.
- الكورسات والدروس والملفات مرتبطة بالتسجيل الفعلي. الطلب المعلق لا يفتح الدروس. الإيقاف يمنع الوصول إلى التعلّم.
- حفظ خطة المذاكرة ونتائج التدريب في `learning_states` للحسابات المسجلة. التخزين المحلي مخصص للزائر وتفضيل المظهر وجلسة المصادقة؛ لا يُنسخ تقدم زائر إلى حساب آخر تلقائيًا.
- المرفقات في bucket خاص `course-materials`، حتى 20MB، PDF وJPEG وPNG وWebP. يتحقق `course-material` من JWT وسياسة الدرس الحالية قبل كل تحميل، ثم يبث الملف باستجابة `no-store`. القراءة المباشرة للـbucket محصورة بالإدارة والخدمة.
- `admin-users` ينشئ حسابات متدربين ويعيد تعيين كلمات مرورهم بعد تحقق الإدارة من الهوية. يمنع إعادة تعيين حساب مدير آخر. الحسابات المنشأة أو المعاد تعيينها مطالبة بتغيير كلمة المرور قبل فتح المحتوى.
- عرض وتعديل المحتوى نصيًا، فيديو YouTube مضمّن من نطاق privacy-enhanced، وروابط HTTPS لمصادر الفيديو الأخرى. لا يُعرض HTML مُدخل بواسطة المستخدم.
- الرسوم دفتر متابعة للتحصيل الخارجي؛ ليست بوابة دفع ولا نظام فواتير ضريبية.

## الجداول

| المجموعة       | الجداول                                                           |
| -------------- | ----------------------------------------------------------------- |
| الحسابات       | `profiles`, `admin_users`                                         |
| التدريب        | `courses`, `cohorts`, `enrollments`, `lessons`, `lesson_progress` |
| متابعة الرسوم  | `invoices`, `payments`                                            |
| التعلّم الشخصي | `learning_states`                                                 |
| التواصل        | `support_tickets`, `announcements`                                |
| المراجعة       | `audit_logs`                                                      |

`private.account_bootstrap` غير مكشوف لواجهة البيانات. يسمح nonce عشوائي قصير الصلاحية ولمرة واحدة بتهيئة حساب المالك من الخدمة دون اختلاق رقم جوال، ولا يمنح صلاحية إدارية. يُستهلك أثناء إنشاء الحساب، وتُمنح الإدارة بعملية منفصلة محمية. لا توجد مفاتيح خدمة في الواجهة أو المستودع.

## العمليات الحساسة

جميع RPC الإدارية تفحص `is_admin()` داخل قاعدة البيانات. الثقة لا تعتمد على إخفاء الأزرار. دوال المتدرب تشتق هويته من `auth.uid()`.

- `request_enrollment`: أُوقف إنشاء الطلبات منها في التحديث الثامن لصالح الحجز عبر واتساب.
- `admin_enroll`: قفل صف الدفعة قبل فحص المقاعد وتعديل التسجيل.
- `admin_set_student_status`, `admin_update_profile`: تعديل الحالة والملف مع حماية حسابات الإدارة وتسجيل الأثر.
- `admin_create_invoice`: إنشاء مطالبة بمبلغ موجب ومنزلتين عشريتين.
- `admin_record_payment`: قفل المطالبة، منع تجاوز المبلغ، ومعرّف idempotency لمنع التكرار. إعادة استخدام المعرّف بقيم مختلفة تُرفض.
- `admin_void_financial_record`: إلغاء منطقي مع حفظ السجل. لا تُلغى مطالبة عليها تحصيل قائم، ولا تحدث عملية رد أموال فعلية.
- `submit_ticket`: خمس رسائل كحد أقصى للحساب في الساعة، مع قفل لمنع تجاوز الحد بالتوازي.
- `admin_reply_ticket`: الرد داخل حساب المتدرب.

تنبيهات Supabase Advisor الخاصة بالدوال `SECURITY DEFINER` المتاحة للدور `authenticated` متوقعة: هذه واجهات RPC مقصودة ومحروسة داخليًا واختُبرت رفضًا وسماحًا. لا توجد صلاحيات تعديل مباشرة للمتدرب على المطالبات أو المدفوعات أو الأدوار. حماية كلمات المرور المسرّبة الإضافية لدى المزود غير مفعلة؛ الحد الأدنى 10 أحرف مع حرف إنجليزي كبير وصغير ورقم.

## الحساب الإداري

البريد المعتمد: `ali212@icloud.com`. بيانات أول دخول محفوظة محليًا في `.secrets/admin-access.txt` بصلاحية 0600 وخارج Git. يفرض النظام تغيير كلمة المرور عند أول دخول. لا تُضمّن هذا الملف في ZIP أو المستودع أو صفحات الموقع.

## إعداد البريد والدفع

التسجيل والدخول بالبريد وكلمة المرور يعملان. لا توجد خدمة SMTP خاصة بالمركز حاليًا؛ لذلك لا تُرسل رسائل تحقق أو استعادة تلقائية. `mailer_autoconfirm=true`، ولا يُعرض البريد على أنه موثّق. الاستعادة حاليًا بواسطة الإدارة بعد التحقق من هوية المتدرب. قبل تفعيل البريد:

1. ربط SMTP مخصص للمركز داخل Supabase.
2. اختبار تفعيل حساب واستعادة كلمة المرور بعناوين خارج فريق المشروع، والتحقق من وصول الرسائل وروابط العودة.
3. تفعيل تأكيد البريد وضبط القوالب العربية.
4. تغيير `VITE_AUTH_EMAIL_ENABLED=true` وإعادة بناء الواجهة ونشرها، وتحديث صفحة الخصوصية.

أي ربط لاحق لبوابة دفع يحتاج مزودًا وحساب تاجر وwebhook محقق التوقيع، مع idempotency وربط المدفوعات بسجل الخادم. لا تعتمد حالة «مدفوع» من معلمات رابط العودة أو من إدخال المتدرب.

## التطوير والتحديث

```sh
npm ci
cp .env.example .env.local
# ضع المفتاح العام للمشروع في .env.local
npm run dev
npm run lint
npm run build
```

الملفات `supabase/migrations/` هي المصدر المرجعي للمخطط. رُبط المشروع وسُجل تاريخ الترحيلات الثماني بعد تطبيقها. لإضافة ترحيلات لاحقة من جهاز مخوّل:

```sh
supabase login
supabase link --project-ref kjmhlixwoafuerhouxvz
supabase db push
supabase functions deploy admin-users --use-api
supabase functions deploy course-material --use-api
```

`verify_jwt=false` في إعداد الوظيفتين متعمد لأنهما تتحققان صراحةً من JWT بواسطة `auth.getUser()` ثم من الدور أو RLS؛ ليس وصولًا عامًا للعمليات. `SUPABASE_SERVICE_ROLE_KEY` متاح داخل بيئة الوظيفة فقط.

الواجهة على GitHub Pages، وخدمات Supabase مستقلة وتعمل عبر HTTPS. تشغيل `npm run deploy:build` يُنتج `docs/`، ثم نشر commit يحتوي تلك المخرجات يشغّل `.github/workflows/pages.yml`. لا يلزم مفتاح خدمة في GitHub Actions.

## التحقق وإدارة البيانات

```sh
node --env-file=.env.local scripts/verify-backend.mjs
```

يتطلب الاختبار مفتاح خدمة للخادم في `SUPABASE_SERVICE_ROLE_KEY` أو الملف المحلي الخاص. ينشئ حسابات وبيانات اختبار مميزة ثم يحذفها. `--keep-fixtures` للاختبار المرئي فقط؛ يلزم تنظيفها قبل التسليم. لا تستخدم بيانات حقيقية في fixtures.

قبل حذف حساب فعلي، راجع السجلات المالية والتزامات الاحتفاظ. الحذف الإداري من مزود المصادقة يُسقط بيانات الحساب المرتبطة بواسطة المفاتيح الخارجية. الواجهة توفر الإيقاف ولا توفر حذفًا شاملًا بضغطة واحدة.

المحتوى التعليمي الأولي 12 درسًا تمهيديًا قابلًا للتحرير ضمن مسارات القدرات؛ توسع الدليل إلى 13 دورة تعريفية في التحديث الثامن. بقية الدورات جاهزة لإضافة الدروس من الإدارة. مواعيد الدفعات وأسعار الكورسات غير مفترضة؛ تضيفها الإدارة حسب البرامج الفعلية.

## تحديث الدليل والحجز — 13 سبتمبر 2026

الترحيل الثامن `202609130008_course_catalog_whatsapp.sql` يضيف تصنيف المجال، عرض الدورة في الدليل، ترتيبها، صورة الغلاف والتفاصيل التعريفية. الدليل العام يعرض 13 دورة، مع إبقاء المسارات الثلاثة الفرعية القديمة للقدرات داخل الإدارة دون عرضها في الدليل. لم تُحذف دروسها أو تسجيلاتها.

`details` يتضمن العبارة التعريفية والنبذة والفئة المستهدفة والمتطلبات والمحاور والمهارات ووصف الصورة. واجهة الإدارة تحفظ هذه الحقول في قاعدة البيانات. الصور الأصلية للدليل موجودة تحت `public/images/courses/`؛ الصور تمثيلية وليست صورًا فعلية للقاعات.

الحجز والتأكيد عبر واتساب **0558815053**. تستمر `admin_enroll` في اعتماد التسجيلات بصلاحية الإدارة. `request_enrollment` القديمة ترفض الطلب مع `BOOKING_VIA_WHATSAPP` ولا تنشئ تسجيلًا. الضغط على رابط واتساب لا يرسل شيئًا تلقائيًا ولا يغيّر بيانات المنصة.

إعدادات الاتصال وحسابات التواصل في `src/services/contact.ts`. حسابا إنستقرام وسناب شات فارغان، ويظهران كأيقونات غير مفعلة لحين إضافة الروابط.

## نموذج الحجز وإزالة الدخول العام — 13 سبتمبر 2026

تجربة الزائر الآن تبدأ من `/booking`؛ روابط الدورات تضيف `?course=COURSE_ID` لاختيار الدورة تلقائيًا. الاسم والجوال والدورة مطلوبة، والبريد والمدينة والملاحظات اختيارية. يدعم الجوال الأرقام العربية والفارسية والإنجليزية والصيغ السعودية المحلية والدولية.

عند المتابعة يُجهّز رابط `wa.me/966558815053` بنص يتضمن بيانات النموذج، ويفتح نافذة واتساب ليُرسل الزائر الرسالة بنفسه. يظهر رابط بديل لفتح الرسالة إذا منع المتصفح النافذة الجديدة. تعديل أي حقل يبطل رابط الطلب السابق. لا توجد كتابة إلى قاعدة البيانات أو تخزين دائم لبيانات النموذج، ولا دفع أو تأكيد تلقائي.

أزيلت روابط الدخول والحساب من التنقل والرئيسية والتذييل والتواصل. تنتقل `/login` و`/register` إلى الحجز؛ لوحة الإدارة محفوظة ومحمية في `/admin` ومدخلها `/admin/login`. الحساب غير الإداري يُرفض ويُسجل خروجه. بقيت بيانات التدريب والصلاحيات السابقة محفوظة؛ لم يلزم ترحيل قاعدة بيانات لهذا التحديث. يحتاج حفظ طلب مؤكد ضمن السجلات الداخلية إلى إدخاله بواسطة الإدارة.

ملف الحجز: `src/pages/Booking.tsx`، تجهيز الرسالة: `src/services/contact.ts`، التنسيق: `src/booking.css`. بيانات دخول المالك المحلية أُبقيت كما هي مع تحديث رابط الدخول إلى المسار الإداري.
