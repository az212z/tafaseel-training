# ربط الواجهة بالباك إند

هذه وثيقة تسليم للمطور. العقود التالية مقترحة ولم تنفذ كخادم خلفي.

## نقطة الربط

1. استبدل `readLearning` و`writeLearning` في `src/services/learning.ts` بطبقة API غير متزامنة.
2. حدّث إدارة الحالة في `src/App.tsx` لدعم تحميل الحساب، وحالة الانتظار، والإخفاق وإعادة المحاولة قبل عرض البيانات الشخصية.
3. احتفظ بعقد `LearningContext` أو استبدله بمكتبة مناسبة لإدارة طلبات الخادم. فصل الحالة الحالية يسمح بإبقاء معظم مكونات العرض.
4. استبدل البيانات التحريرية في `src/data/content.ts` باستجابات الخادم أو CMS بعد اعتمادها.
5. اربط نموذج `Contact.tsx` بإنشاء طلب فعلي، وغير وصف زر الحفظ ورسالة النجاح فقط بعد تحقق الخادم من استلام الطلب.

## عقود API مقترحة

| الوظيفة         | العقد المقترح                                 | ملاحظات                                          |
| --------------- | --------------------------------------------- | ------------------------------------------------ |
| البرامج         | `GET /api/programs`                           | العنوان، النوع، المحاور، المستوى، الوصف، الإتاحة |
| برنامج واحد     | `GET /api/programs/:id`                       | أضف الجلسات، الرسوم والمدرب عند اعتمادها         |
| بيانات المتدرب  | `GET /api/me`                                 | تتطلب جلسة مسجلة                                 |
| المحفوظات       | `GET /api/me/saved-programs`                  | مسارات تخص المستخدم المصادق عليه                 |
| حفظ/إلغاء مسار  | `PUT /api/me/saved-programs/:id` / `DELETE`   | تجنب الاعتماد على معرف مستخدم يرسله العميل       |
| إنشاء محاولة    | `POST /api/attempts`                          | الخادم يحدد الأسئلة ومعرف المحاولة               |
| إجابة سؤال      | `POST /api/attempts/:id/answers`              | تصحيح الإجابة وإرجاع شرحها من الخادم             |
| نتائج المحاولات | `GET /api/me/attempts`                        | تاريخ، عدد أسئلة، نتيجة، مهارات تحتاج مراجعة     |
| خطة المذاكرة    | `GET /api/me/study-plan` / `PATCH`            | حفظ التقدم وربطه بصاحب الحساب                    |
| الاستفسارات     | `POST /api/inquiries`                         | اسم، جوال اختياري، مسار، نص الاستفسار            |
| المقالات        | `GET /api/articles` / `GET /api/articles/:id` | محتوى معتمد وتاريخ تحديث                         |

## شكل البيانات الحالي

```ts
type Attempt = {
  id: string
  date: string
  questionIds: string[]
  answers: number[]
}

type Draft = {
  name: string
  phone: string
  program: string
  message: string
  savedAt: string
}
```

أنواع `Course` و`Question` و`Article` في `src/data/content.ts`، وأنواع الحالة الكاملة في `src/services/learning.ts`.

## ما يتغير عند تشغيل الخدمات الحقيقية

- مساحة المتدرب الحالية تجربة بلا تسجيل. أضف مصادقة فعلية قبل تحميل بيانات الحساب من الخادم.
- لا تثق في نتيجة يحسبها المتصفح. الأسئلة الحالية وشروحاتها عامة؛ الاختبارات المدفوعة أو المقيمة تحتاج تصحيحًا ومفاتيح إجابة على الخادم.
- تحقق من جميع المدخلات على الخادم، وحدد صلاحيات المستخدم وملكية سجلاته.
- اعرض نجاح إرسال الاستفسار بعد نجاح الطلب فقط، واحتفظ بإمكانية إعادة المحاولة عند إخفاق الشبكة.
- لا تضع أسرار خدمات أو مفاتيح خاصة في متغيرات `VITE_*` لأنها تصل إلى المتصفح.
- حدّث صفحة الخصوصية وقنوات الدعم قبل جمع بيانات حقيقية أو إضافة دفع.
- إذا أضفت حسابات حقيقية، أتح خيار استيراد التقدم المحلي بموافقة المستخدم، ولا تخلط بيانات جهاز مشترك تلقائيًا مع الحساب.
- أضف لوحة الإدارة وإدارة المحتوى والجلسات والدفع كوظائف خلفية منفصلة عند تحديدها.

## الاستضافة

الناتج الحالي ملفات ثابتة في `dist/` وبمسارات نسبية. لا توجد خدمات خارجية مطلوبة ولا خادم API ضمن هذه النسخة. للنشر داخل مجلد فرعي، ارفع محتويات `dist/` مع الحفاظ على مجلدي `assets/` و`brand/`.
